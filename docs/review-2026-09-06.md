# Release review — 2026-09-06

Verified merged main commit `0a77693` together with the preserved uncommitted review fixes. The 0.1.0 release candidate includes optional country names, a compact sidebar, responsive layouts, historical progress and cancellation, source uncertainty and custom transects. PR #1 removed the Saved investigations panel; existing database records remain intact.

## Review fixes

- Historical results now sort equal timestamps by event ID. Repeated unchanged responses retain canonical event ordering and the same content-addressed snapshot ID. A Go regression test checks ordering and repeated retrieval identity.
- Superseded frontend requests stop after response decoding, before clearing the active request's progress polling.
- The extracted-package consumer check compares embedded HTML and production assets byte-for-byte with the current build and checks the runtime version against package.json.

## Verification

Executed successfully on macOS Apple silicon:

- 23 frontend unit tests, TypeScript checking, and Prettier checking.
- `go test -race ./...` and `go vet ./...` (Go reported cached test results).
- Production frontend build and isolated Chromium browser suite: invalid shared coordinates and snapshots, pagination, pending retrieval versus snapshot import, exact historical retry, committed queries, absence of the removed investigations panel, source uncertainty, custom transects, and country-name toggles, hemisphere visibility and shared-link persistence.
- Desktop and mobile automated checks: animated nonblank canvas, no horizontal overflow, zero axe violations, and no page errors. Desktop and mobile full-page screenshots were visually inspected; automated layout checks also covered short desktops and Windows-style scaled viewport sizes on macOS Chromium.
- Fresh Apple silicon ZIP extraction and native executable launch: embedded asset/version consistency, 618-event offline dataset, SQLite readiness, rejected unauthorized quit, backup while running, restart identity, restored database integrity and restored launch. See `consumer-test-results.json`.

## Limits

Browser checks use controlled fixtures and do not establish live-provider acceptance. Intel macOS and Windows runtime execution, signing/notarization, assistive-technology acceptance and independent scientific review remain outstanding. See `release-readiness.md` for the complete remaining release gates.

## GitHub Actions

The merged commit run `34043139255` did not execute test steps. GitHub reports hosted runners are disabled for this repository and directs the owner to their GitHub Enterprise administrator. This is an infrastructure block, not a failing application assertion. Cross-platform CI acceptance remains unverified.
